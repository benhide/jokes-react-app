import React from 'react';

import styles from './Components.module.css';

export const PostJoke = (): JSX.Element => {
  return <p className={styles.infoText}>Sorry this page is still under construction please come back later ...</p>;
};
